const Home = () => {
  // const { user } = useContext(AuthContext);

  return (
    <div>
      <p>Home</p>
      {/* <p>Welcome {user?.user_metadata.full_name}</p> */}
    </div>
  );
};

export default Home;
